<odoo>
  <data>

    <record id="view_move_form_inherit_emission_dt" model="ir.ui.view">
      <field name="name">account.move.form.emission.datetime</field>
      <field name="model">account.move</field>
      <field name="inherit_id" ref="account.view_move_form"/>
      <field name="arch" type="xml">

        <xpath expr="//label[@for='invoice_date']" position="attributes">
          <attribute name="invisible">1</attribute>
        </xpath>
        <xpath expr="//field[@name='invoice_date']" position="attributes">
          <attribute name="invisible">1</attribute>
        </xpath>
        <xpath expr="//field[@name='invoice_date']" position="after">
          <field name="x_emission_datetime"
                 widget="datetime"
                 readonly="state != &quot;draft&quot;"
                 invisible="state != &quot;draft&quot; and x_emission_datetime == False"/>
        </xpath>

      </field>
    </record>

  </data>
</odoo>